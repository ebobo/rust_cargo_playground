use std::cmp::Ordering;

fn main() {
let arr = [1, 2, 3, 4, 5];


println!("1st element of the array is {}", arr[0]);
println!("2nd element of the array is {}", arr[1]);
println!("length of the array is {}", arr.len());



let mut index = 0;
loop {
    // println!("the value is: {}", arr[index]);
    // index = index + 1;
    print!("looping... ");
    // if index == arr.len() {
    //     break;
    // }
}
}